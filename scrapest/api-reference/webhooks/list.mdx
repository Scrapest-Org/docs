---
title: "Get Webhook"
api: "GET https://scrapest.duckdns.org/webhook"
---

Retrieve the registered webhook for your API key.

<ParamField header="x-api-key" type="string" required>
  Your unique API key.
</ParamField>

<ResponseField name="data" type="string">
  The registered webhook URL, or null if none is registered.
</ResponseField>

<RequestExample>
  ```bash curl
  curl -X GET https://scrapest.duckdns.org/webhook \
  -H "x-api-key: YOUR_API_KEY"
  ```

```javascript fetch
fetch("https://scrapest.duckdns.org/webhook", {
  method: "GET",
  headers: {
    "x-api-key": "YOUR_API_KEY",
  },
})
  .then((response) => response.json())
  .then((data) => console.log(data));
```

</RequestExample>

<ResponseExample>
  ```json Response
  {
    "data": "https://your-webhook.com/endpoint"
  }
  ```
</ResponseExample>
