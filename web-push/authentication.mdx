---
title: "Authentication"
description: "Authenticating requests to the Web Push Service"
---

Most endpoints in the Web Push Service are protected and require authentication.

## API Key

To authenticate your requests, you must include the `x-api-key` header with a valid API key.

<ParamField header="x-api-key" type="string" required>
  Your unique API key generated via the `/auth` endpoint.
</ParamField>

## Generate API Key

To get started, you need to generate an API key via our **Telegram Bot**.
**Note:** There is a strictly enforced limit of **3 API keys** per Telegram user.

1.  Call the `/auth` endpoint to initiate the process.

```bash
curl -X POST https://webpush-production.up.railway.app/auth
```

### Response

The API will return an authentication link:

```json
{
  "status": "success",
  "message": "Please authenticate via Telegram",
  "data": {
    "auth_url": "https://t.me/Scrapest_Bot?start=123abc456..."
  }
}
```

2.  **Click the `auth_url`** to open the Telegram Bot.
3.  Click **Start** or send `/start ...`.
4.  The bot will verify your session and send you a new **API Key**.

> Keep this key safe! You can list your active keys at any time by sending `/getkey` to the bot.

## Live Demo

You can interact with our live Web Push instance at:

`https://webpush-production.up.railway.app/`

<Note>
  Authentication is required for all state-changing operations and sensitive
  data access on the live instance.
</Note>

### Example Request

<CodeGroup>

```bash curl
curl -X GET https://webpush-production.up.railway.app/health \
  -H "x-api-key: your-api-key"
```

```javascript fetch
fetch("https://webpush-production.up.railway.app/health", {
  method: "GET",
  headers: {
    "x-api-key": "your-api-key",
  },
})
  .then((response) => response.json())
  .then((data) => console.log(data));
```

```python python
import requests

url = "https://webpush-production.up.railway.app/health"
headers = {
    "x-api-key": "your-api-key"
}

response = requests.get(url, headers=headers)
print(response.json())
```

```go go
package main

import (
	"fmt"
	"io/ioutil"
	"net/http"
)

func main() {
	url := "https://webpush-production.up.railway.app/health"
	method := "GET"

	client := &http.Client{}
	req, err := http.NewRequest(method, url, nil)

	if err != nil {
		fmt.Println(err)
		return
	}
	req.Header.Add("x-api-key", "your-api-key")

	res, err := client.Do(req)
	if err != nil {
		fmt.Println(err)
		return
	}
	defer res.Body.Close()

	body, err := ioutil.ReadAll(res.Body)
	if err != nil {
		fmt.Println(err)
		return
	}
	fmt.Println(string(body))
}
```

```rust rust
use reqwest::header;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let client = reqwest::Client::new();

    let res = client
        .get("https://webpush-production.up.railway.app/health")
        .header("x-api-key", "your-api-key")
        .send()
        .await?
        .text()
        .await?;

    println!("{}", res);
    Ok(())
}
```

</CodeGroup>

### Error Responses

- **401 Unauthorized**: Missing or invalid `x-api-key`.
- **403 Forbidden**: API key limit reached (when generating keys).

```json 401 Unauthorized
{
  "status": "error",
  "error": "Missing or invalid x-api-key header"
}
```
